# NEED TO HANDLE MISSING PARAMETERS FROM TRANSFORMATION

NEED TO HANDLE MISSING PARAMETERS FROM TRANSFORMATION

## Usage

``` r
transformTable(
  input,
  transformation = NULL,
  transform_type = c("logicle", "arcsinh", "other", "none"),
  find_inverse = FALSE
)
```
