# filterData.data.frame

filterData.data.frame

## Usage

``` r
# S3 method for class 'data.frame'
filterData(input, clusters = NULL, metaclusters = NULL)
```

## Value

A tidytable, where only cells belonging to the given clusters or
metaclusters are included.
