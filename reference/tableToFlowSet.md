# tableToFlowSet

Convert a table of single-cell data !!! from here, add function to save
.fcs files

## Usage

``` r
tableToFlowSet(table, id_col = .id)
```

## Arguments

- table:

  The data table to convert to a flowSet

- id_col:

  The column containing sample IDs for the experiment

## Value

A flowSet
