# prepareCompensationMatrix

Changes columns of compensation matrix to channels, markers, or both
(pretty) and checks order of columns

## Usage

``` r
prepareCompensationMatrix(matrix, gs)
```

## Arguments

- matrix:

  The compensation matrix to prepare

- gs:

  The `GatingSet` whose parameter names the compensation matrix's
  columns should be matched to
